function Pause-Work { Write-Host "Pause-Work (stub) – ยังไม่ได้ติดตั้งสคริปต์จริง" -ForegroundColor Yellow }
function Resume-Work { Write-Host "Resume-Work (stub) – ยังไม่ได้ติดตั้งสคริปต์จริง" -ForegroundColor Yellow }
